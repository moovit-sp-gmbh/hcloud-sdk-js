export enum Level {
    INFO = "info",
    WARN = "warn",
    ERROR = "error",
    DEBUG = "debug",
}

export enum Products {
    idp = "idp",
    auditor = "auditor",
    mailer = "mailer",
    high5 = "high5",
    all = "all",
}

export enum Entity {
    controller = "controller",
    service = "service",
    util = "util",
    helper = "helper",
    interceptor = "interceptor",
}
