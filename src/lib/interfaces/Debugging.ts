export enum Level {
    INFO = "info",
    WARN = "warn",
    ERROR = "error",
    DEBUG = "debug",
}

export enum Entity {
    controller = "controller",
    service = "service",
    util = "util",
    helper = "helper",
    interceptor = "interceptor",
    database = "database",
    module = "module"
}
